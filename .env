# Local Development Environment Variables
# This file is used for local development with npm run start:dev

# Application Configuration
PORT=3000
NODE_ENV=development
API_PREFIX=/api/v1

# Database Configuration (Local PostgreSQL)
DATABASE_URL=postgresql://postgres:123456@localhost:5432/backend_db?schema=public

# Redis Configuration (Local Redis)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration (Development keys - change for production!)
JWT_SECRET=dev-secret-key-change-in-production-min-32-characters
JWT_REFRESH_SECRET=dev-refresh-secret-key-change-in-production-min-32

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true
FRONTEND_URL=http://localhost:3000

# OpenAI Configuration (Optional - add your key if needed)
# OPENAI_API_KEY=your_openai_api_key

# Anthropic Configuration (Optional - add your key if needed)
# ANTHROPIC_API_KEY=your_anthropic_api_key

# Email Configuration (Optional - add if you need email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASSWORD=your_email_password

# Sentry Configuration (Optional - add for error tracking)
# SENTRY_DSN=your_sentry_dsn_here
